# Same values as backend/.env - this app calls the backend's service
# functions directly (see design.md Decision #1), so it needs the same
# database and LLM credentials.
DATABASE_URL=postgres://user:password@host:5432/dbname
GEMINI_API_KEY=...
# GEMINI_MODEL=gemini-flash-latest
